apiVersion: v1
kind: Service
metadata:
  name: {{ include "streaming-api.fullname" . }}
  labels:
    {{- include "streaming-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "streaming-api.selectorLabels" . | nindent 4 }}
