# Copiar a /etc/raspi-streaming/ngrok.yml y reemplazar el authtoken.
# No subir este archivo con un token real.

version: 3
agent:
  authtoken: <your-authtoken>

tunnels:
  web:
    proto: http
    addr: https://127.0.0.1:8443
  ssh:
    proto: tcp
    addr: 22
